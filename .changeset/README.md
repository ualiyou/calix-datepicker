# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` to record a change. Each release, `pnpm version-packages`
consumes these and bumps versions; CI publishes to npm via the release workflow.

See the [common questions](https://github.com/changesets/changesets/blob/main/docs/common-questions.md).
