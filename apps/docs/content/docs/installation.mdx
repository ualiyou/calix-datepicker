---
title: Installation
description: Install Calix and its adapters.
---

Install the React binding and at least one calendar adapter. Add the optional
theme package for default styling.

<Tabs items={["pnpm", "npm", "yarn"]}>

```bash tab="pnpm"
pnpm add @alydev/datepicker @alydev/adapter-gregorian @alydev/themes
```

```bash tab="npm"
npm install @alydev/datepicker @alydev/adapter-gregorian @alydev/themes
```

```bash tab="yarn"
yarn add @alydev/datepicker @alydev/adapter-gregorian @alydev/themes
```

</Tabs>

Need the Persian calendar too?

```bash
pnpm add @alydev/adapter-jalali
```

## Peer dependencies

`@alydev/datepicker` expects **React 18.3+ or 19** (`react` and `react-dom`). The
Gregorian adapter depends on `date-fns`; the Jalali adapter on `date-fns-jalali`.
These are pulled in automatically.

## Import the theme (optional)

```ts
import "@alydev/themes/default.css"; // full theme
// or
import "@alydev/themes/minimal.css"; // structural only
```

The library works completely unstyled too — see the [Theming guide](/docs/theming).
